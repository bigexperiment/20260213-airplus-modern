/** Open Graph / Twitter card — platform standard (1.91:1) */
export const OG_WIDTH = 1200;
export const OG_HEIGHT = 630;

/** Render at 2× then downscale for sharper text and smoother photos */
export const OG_RENDER_SCALE = 2;

export const OG_RENDER_WIDTH = OG_WIDTH * OG_RENDER_SCALE;
export const OG_RENDER_HEIGHT = OG_HEIGHT * OG_RENDER_SCALE;
